# Environment Configuration for Digital Wallet System
# Copy this file to .env and adjust values for your environment

# ==================== Spring Profile ====================
SPRING_PROFILES_ACTIVE=dev

# ==================== Local Development Database ====================
DB_PASSWORD=your_local_postgres_password
DB_USERNAME=postgres

# ==================== JWT ====================
# Generate with: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_at_least_64_characters_long

# ==================== Frontend ====================
FRONTEND_URL=http://localhost:3000
VITE_API_URL=/api/v1

# ==================== Email (Dev - MailHog) ====================
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_AUTH=false
MAIL_STARTTLS=false

# ==================== Email (Prod - Testmail.app) ====================
# Get credentials from testmail.app (GitHub Student Pack)
# MAIL_HOST=smtp.testmail.app
# MAIL_PORT=587
# MAIL_USERNAME=your_testmail_api_key
# MAIL_PASSWORD=your_testmail_api_key
# MAIL_AUTH=true
# MAIL_STARTTLS=true

# ==================== Production (Supabase) ====================
# Uncomment and configure for production deployment
#
# DATABASE_URL=jdbc:postgresql://db.[PROJECT_REF].supabase.co:5432/postgres
# DB_USERNAME=postgres
# DB_PASSWORD=your_supabase_password
#
# For connection pooling (recommended for serverless/Heroku):
# DATABASE_URL=jdbc:postgresql://db.[PROJECT_REF].supabase.co:6543/postgres?pgbouncer=true

